### To start processing file with log run following command from project root directory:
###### `${path}` should be replaced with proper path to log file.

```shell script
gradle bootrun -Pargs=${path} 
```

